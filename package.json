{
  "name": "pi-cover-image",
  "version": "0.3.1",
  "description": "Cover image pipeline for articles: idea-extractor (3 candidates, scored) → palette/layout planners → artist-works → feature extraction → prompt-composer → generation via pi-image-gen. Surface-driven (X, WeChat, Xiaohongshu, generic ratios).",
  "keywords": ["pi-package"],
  "license": "MIT",
  "pi": {
    "skills": ["./skills", "./node_modules/pi-image-gen/skills"],
    "subagents": { "agents": ["./agents"] }
  },
  "dependencies": {
    "pi-image-gen": "github:WeZZard/pi-image-gen",
    "pi-subagents": "^0.36.0"
  },
  "bundledDependencies": ["pi-image-gen"]
}