# Surface: 1:1 (square)

Generic square. Defaults below; refine on first real use.

## Config

- `aspectRatio`: 1:1
- `safeArea`: full
- `bleed`: full
- `text`: optional
- `cropBehavior`: none
- `filename`: cover-image-1x1.png

## Generation

- One 1:1 image: style **visual language** + rhetoric, full-bleed. Add a title only when the surface choice explicitly asks for text.
- Save as `cover-image-1x1.png` in the output directory.